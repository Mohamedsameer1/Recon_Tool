# subscan has no third-party Python dependencies — it only uses the
# standard library (argparse, socket, urllib, json, csv, re,
# concurrent.futures, subprocess).
#
# Optional external tool (not a pip package):
#   subfinder — required only if you use the -s / --subfinder flag
#   Install: https://github.com/projectdiscovery/subfinder#install
